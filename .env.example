# Example environment variables for BugScribe application.

# Server Port
PORT=3001

# JWT secret - set this to a strong random value in production.
# Generate with node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET= Generate a strong random value here

# Set DB provider to 'sqlite' or 'postgres'
#DB_PROVIDER=postgres
#DB_PROVIDER=sqlite

# If using sqlite, DB_DATABASE can be a relative path (relative to repo root) or absolute
#DB_DATABASE=data/db.sqlite

# If using Postgres, you can set DATABASE_URL or individual DB_* vars
#DATABASE_URL=postgres://user:password@localhost:5432/BugScribe
#DB_HOST=127.0.0.1
#DB_PORT=5432
#DB_USER=postgres
#DB_PASSWORD=postgres
#DB_DATABASE=BugScribe